---
title: "Introduction"
description: "Lemma is an observability platform for AI agents"
---

Lemma is an observability platform for AI agents. You send one trace per agent execution, and Lemma lets you search, debug, and monitor what your agents did in production, and automatically surfaces silent failures.

You instrument with the Lemma SDK; Lemma reads the result and owns the analysis. Follow Lemma’s [trace contract](/reference/trace-contract) so every run is complete and easy to debug.

<Note>
  **One agent execution = one trace.** LLM calls and tool calls are child spans of that trace, not separate traces.
</Note>

## Get started

<CardGroup cols={2}>
  <Card title="Quickstart" icon="/icons/rocket.svg" href="/getting-started/quickstart">
    Install the SDK, point it at Lemma, and send your first trace in a few minutes.
  </Card>
  <Card title="Building high-quality traces" icon="/icons/circle-check.svg" href="/guides/building-high-quality-traces">
    The ideal instrumentation path, with bad → better → best examples.
  </Card>
</CardGroup>

## How it works

1. **Instrument with the SDK**: wrap one agent execution in `lemma.trace()`, and record generation, tool, and app work inside it.
2. **Send over HTTPS**: the SDK delivers the trace to Lemma using your Lemma credentials.
3. **Lemma reads the trace shape**: it shows inputs and outputs, model calls, tool calls, threads, and runs automated issue detection.

## What to instrument

Instrument every part of your agent. Each layer unlocks more of Lemma, and a complete trace is what powers full debugging and automated issue detection. Aim to record all of them.

| Layer | Record this | You can now see |
| --- | --- | --- |
| [Trace](/tracing/instrumentation/traces) | Root with input/output | Which executions ran and whether they succeeded |
| [Generations](/tracing/instrumentation/generations) | Model, full messages, completion | Prompts, completions, model, and latency |
| [Tool calls](/tracing/instrumentation/tool-calls) | Name, arguments, result | Tool arguments, results, errors, and latency |
| [Spans](/tracing/instrumentation/spans) | Retrieval, ranking, app logic | Every app-logic step in the execution tree |
| [Threads & context](/tracing/instrumentation/context) | Thread, user, metadata | Conversations, users, custom filters |

See [Building high-quality traces](/guides/building-high-quality-traces) for what a complete, well-instrumented trace looks like.

## What you can do with Lemma

Lemma covers three core workflows:

- **Trace agents**: see the full execution tree for every trace, including LLM calls, tools, retrieval, latency, and errors.
- **Debug failures**: inspect inputs, outputs, and errors across nested spans; filter by user, session, and custom attributes.
- **Connect your IDE**: use the [Lemma MCP server](/connections/mcp) to query traces from Cursor, Claude Desktop, or Claude Code.

## Choose your path

<CardGroup cols={2}>
  <Card title="Instrument with the SDK" icon="/icons/wrench.svg" href="/tracing/instrumentation/setup">
    Install the SDK, then add traces, generations, tool calls, and spans.
  </Card>
  <Card title="Building high-quality traces" icon="/icons/circle-check.svg" href="/guides/building-high-quality-traces">
    The ideal instrumentation path, with bad → better → best examples.
  </Card>
</CardGroup>

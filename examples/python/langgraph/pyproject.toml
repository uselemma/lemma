[project]
name = "lemma-example-langgraph"
version = "0.0.0"
description = "Lemma docs agent instrumented with LangGraph"
requires-python = ">=3.10"
dependencies = [
    "langchain-core>=0.3.0",
    "langchain-openai>=0.3.0",
    "langgraph>=0.2.0",
    "lemma-examples-shared",
    "uselemma-tracing[langgraph]",
]

[tool.uv]
package = false

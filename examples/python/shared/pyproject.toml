[project]
name = "lemma-examples-shared"
version = "0.0.0"
description = "Shared docs-agent helpers for Lemma examples"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lemma_examples_shared"]
